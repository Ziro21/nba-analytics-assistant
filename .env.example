# Optional — only needed to run the assistant in LLM mode (`python main.py --mode llm "..."`).
# The default rule-based mode runs with NO API key. Copy this file to `.env` and fill in a key
# only if you want to try the optional LLM tool-calling parser. `.env` is git-ignored.
OPENAI_API_KEY=
