# Virtual environments
.venv/
venv/
env/

# Python caches / build
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Secrets (never commit real keys)
.env

# Private working notes / phase reviews (must NOT ship)
_working/

# Source/reference materials kept on disk but NOT part of the shipped deliverable
# (see plan §5 "what ships"). Reversible — remove these lines to include them.
take_at_home_task.pdf
*Development Plan*.md

# Paused UI-phase planning notes (UI work is deferred; these resume with that phase, not now).
# Kept on disk, deliberately untracked. Reversible — remove these lines to track them.
docs/pre_ui_core_improvement_recommendation.md
docs/ui_implementation_plan.md
docs/ui_recommendation.md
docs/ui_research.md

# OS noise
.DS_Store
